spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/factoryflow
    username: postgres
    password: Momdad@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

agent:
  service:
    url: http://localhost:8000

logging:
  level:
    com.factoryflow: DEBUG
    org.springframework.web: INFO
